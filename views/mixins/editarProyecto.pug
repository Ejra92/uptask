mixin editarProyecto(proyecto= {})

    form.agregar-proyecto(action=`/nuevo-proyecto/${proyecto.id || ""}` method="POST")
        .campo
            label(for="nombre") Nombre Proyecto:
            input(type="text" id="nombre" value=`${proyecto.nombre || ""}` name="nombre" placeholder="Nombre del proyecto")
        .campo.enviar
            input(type="submit" value=`${proyecto.nombre ?'Editar':'Guardar'}` class="boton")

            //-diciendo mixin, le indicamos que esto es un mixin, () ira el parametro que inicia como un objeto vacio para no colocar nada mas