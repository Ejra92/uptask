<!DOCTYPE html>
html(lang="en")
    head
        //de esta forma incluimos el partial que lo que hace es tener en comun el head de todos los html
        include partials/header
    body.login
        //aqui damos la estructura que tendra nuestro formulario de crear cuenta
        .contenedor-formulario
            h1 Crea tu Cuenta de uptask
            include partials/flash

            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") Email
                    input(type="email" name="email" placeholder="Correo" value=email)
                .campo
                    label(for="password") Password
                    input(type="password" name="password" placeholder="Contraseña" value=password)
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a iniciar sesion
